using wsl
  - ```sudo apt update```
  - install brew
    - ```sudo apt-get install build-essential procps curl file git```
    - ```/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"```
    - 
  - follow instruction at https://learn.adafruit.com/building-circuitpython/build-circuitpython
  - and https://learn.adafruit.com/building-circuitpython/espressif-build
  
  - pip install huffman
  - make -j16 -C mpy-cross
  - sudo apt install python3.10-venv
  - cd ports/espressif/
  - esp-idf/install.sh 
  - ```. esp-idf/export.sh```
  - pip install minify_html jsmin